# JSON Comparator Service

The purpose of this application is store two JSON Strings and get the difference thru REST API.

## Running locally

- Using Maven:
```
    mvn spring-boot:run
```

- Packaging and Testing:
```
    mvn clean package
```